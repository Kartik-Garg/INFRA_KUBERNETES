1) when using config file to create secrets, the values need to be converted to base64 in data field, a simple way of doing this can be:
    echo -n 'string' | base64
    this output can be then stored in the yaml file 